[project]
name = "mado-queue"
version = "0.1.0"
description = "MADO queue"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "Flask>=3.0.0",
    "flask-cors>=4.0.0",
    "pyusb>=1.0.0",
    "pywin32>=306; platform_system == 'Windows'",
    "waitress>=3.0.0",
]
